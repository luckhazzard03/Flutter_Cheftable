name: flutter_application_5
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.18.0 # Asegúrate de usar la última versión disponible
  cupertino_icons: ^1.0.6
  http: any
  shared_preferences: ^2.0.13
dev_dependencies:
  flutter_test:
    sdk: flutter
  crypto: ^3.0.1
  flutter_secure_storage: ^6.1.0
  #crypto: ^3.0.1 encripta la contraseña
  shared_preferences: ^2.0.15 # Asegúrate de usar la última versión disponible

flutter:
  uses-material-design: true

  # Aquí agregamos la sección de assets
  assets:
    - assets/img/logo.png
    - assets/img/rest.jpg
    - assets/img/logo2.png
    - assets/img/platos.jpg
